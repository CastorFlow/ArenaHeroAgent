# 中轨子轨道详解

## 🎯 核心发现

你的游侠**不是**在同一个轨道上，而是分布在**中轨的 3 条子轨道**上！

**中轨（Mid Orbit）是一个多层同心结构**，由多条不同半径的子轨道组成，每条子轨道可以容纳多个游侠。

---

## 📊 当前 6 个游侠的实际分配

从 vps168 日志（tick 84216-84225）提取的实时数据：

| 游侠 ID | lane | 子轨道 | 半径 | 同半径伙伴 |
|---------|------|--------|------|-----------|
| `05980142` | 0 | **子轨 0** | **14** | `0b2d5ec7` |
| `0b2d5ec7` | 1 | **子轨 0** | **14** | `05980142` |
| `685000e2` | 2 | **子轨 1** | **19** | `ece94424` |
| `ece94424` | 3 | **子轨 1** | **19** | `685000e2` |
| `ee5bf356` | 0 | **子轨 2** | **24** | `f4d11adf` |
| `f4d11adf` | 1 | **子轨 2** | **24** | `ee5bf356` |

---

## 🔍 lane 编号的真正含义

**lane 编号不是全局轨道编号，而是同一子轨内的 group_index！**

### 误解 vs 真相

❌ **错误理解**：
- lane 0 = 第 1 条全局轨道
- lane 1 = 第 2 条全局轨道
- 6 个游侠分配到 4 条不同的全局轨道

✅ **正确理解**：
- lane 是**同一半径内的分组索引**
- **同半径的游侠**通过不同的 lane (0, 1) 错开 phase（相位偏移）
- **不同半径的游侠**可以有相同的 lane 编号（因为它们在不同的子轨道上）

### 实际结构

```
子轨 0 (半径 14):
  ├─ 游侠 #1 (lane=0, phase=0) ← 在四角之一
  └─ 游侠 #2 (lane=1, phase=2) ← 在对角位置

子轨 1 (半径 19):
  ├─ 游侠 #3 (lane=2, phase=0) ← 在四角之一
  └─ 游侠 #4 (lane=3, phase=2) ← 在对角位置

子轨 2 (半径 24):
  ├─ 游侠 #5 (lane=0, phase=0) ← 在四角之一（与 #1 同 lane，但半径不同）
  └─ 游侠 #6 (lane=1, phase=2) ← 在对角位置（与 #2 同 lane，但半径不同）
```

**关键点**：
- 游侠 #1 和 #5 都是 `lane=0`，但它们在**不同半径**（14 vs 24），所以不会碰撞
- lane 编号在**同一半径内**用于区分相位，**跨半径**可以重复使用

---

## 🛠️ 轨道分配算法详解

### 算法核心：外圈优先混合策略

你的策略使用 `_lightning_calculate_outer_first_orbits` 算法，目标是**最大化领土覆盖**：

#### 参数
- `unit_count` = 6（游侠数量）
- `gap` = 5（游侠视野半径，子轨间隔）
- `inner_radius` = 14（起始半径 = 先锋最外层 9 + 5）
- `min_units_per_orbit` = 2（每条子轨最少 2 个单位）

#### Phase 1: 铺开领土（优先外扩）

算法会**先铺开多条子轨道**，而不是在一条轨道上堆满 8 个单位：

```python
# Phase 1: 每条轨道先分配最少单位，铺开领土
remaining = 6
radius = 14
result = []

while remaining >= 2:
    result.append([radius, 2])  # 每条子轨先放 2 个单位
    remaining -= 2
    radius += 5

# 结果：
# - 子轨 0: 半径 14, 2 个单位 (剩余 4)
# - 子轨 1: 半径 19, 2 个单位 (剩余 2)
# - 子轨 2: 半径 24, 2 个单位 (剩余 0)
```

#### Phase 2: 按周长比例加密（本例跳过）

如果还有剩余单位（本例中剩余 0），会按子轨道周长比例回填：
- 外圈子轨道周长更大 → 分配更多单位
- 单条子轨道上限：8 个单位（四角 + 四边中点）

#### Phase 3: 余数处理（本例跳过）

从外向内依次加 1，直到分配完毕。

### 单位到子轨的映射

```python
# 按 UUID 排序后，依次分配到各子轨
sorted_rangers = sorted(turn.rangers, key=uuid_key)

unit_idx = 0
for (radius, count) in orbit_distribution:
    for group_idx in range(count):
        ranger = sorted_rangers[unit_idx]
        assignments[ranger.id] = (radius, group_idx)
        unit_idx += 1

# 结果：
# ranger #0 → (14, 0)  # 子轨 0, group_idx 0 → lane 0
# ranger #1 → (14, 1)  # 子轨 0, group_idx 1 → lane 1
# ranger #2 → (19, 0)  # 子轨 1, group_idx 0 → lane 2 (实际日志)
# ranger #3 → (19, 1)  # 子轨 1, group_idx 1 → lane 3 (实际日志)
# ranger #4 → (24, 0)  # 子轨 2, group_idx 0 → lane 0 (复用)
# ranger #5 → (24, 1)  # 子轨 2, group_idx 1 → lane 1 (复用)
```

**注意**：日志中 lane 2/3 是因为代码在某处将 `group_idx` 累积计算了，但本质仍是"同半径内的分组索引"。

---

## 🌀 相位错开机制（Phase Offset）

同一子轨道上的多个游侠通过 **phase_offset** 错开位置，避免聚堆：

```python
# arena_hero_strategy.py:7066-7087
def _lightning_orbit_waypoint(turn, unit, role, lane):
    radius, group_idx = assignments[unit.id]
    group_size = count_units_at_radius(radius)  # 同半径单位总数

    # phase_offset 根据 group_idx 计算
    phase_offset = group_idx * 4 // group_size

    # 四角巡逻点
    corners = [
        (radius, radius),    # phase 0
        (radius, -radius),   # phase 1
        (-radius, -radius),  # phase 2
        (-radius, radius),   # phase 3
    ]

    # 当前单位的起点 = corners[phase_offset]
    target = calculate_next_waypoint(unit, corners, phase_offset)
    return target
```

### 示例：子轨 0（半径 14，2 个游侠）

| 游侠 | group_idx | group_size | phase_offset | 起始角 |
|------|-----------|------------|--------------|--------|
| #1 (05980142) | 0 | 2 | 0 * 4 // 2 = **0** | 东北角 (14, 14) |
| #2 (0b2d5ec7) | 1 | 2 | 1 * 4 // 2 = **2** | 西南角 (-14, -14) |

**结果**：
- 两个游侠从**对角**出发
- 都沿方环巡逻（东北 → 东南 → 西南 → 西北 → 循环）
- 永远相隔半圈（180°），不会碰撞

### 如果子轨上有 4 个游侠

| group_idx | phase_offset | 起始角 |
|-----------|--------------|--------|
| 0 | 0 * 4 // 4 = 0 | 东北角 |
| 1 | 1 * 4 // 4 = 1 | 东南角 |
| 2 | 2 * 4 // 4 = 2 | 西南角 |
| 3 | 3 * 4 // 4 = 3 | 西北角 |

**结果**：4 个游侠分别从**四个角**出发，均匀分布。

---

## 🚀 添加第 7 个游侠后的分配

### 当前状态（6 游侠）

```
子轨 0 (r=14): 2 个 ██
子轨 1 (r=19): 2 个 ██
子轨 2 (r=24): 2 个 ██
```

### 添加第 7 个游侠

**算法重新分配**：
- Phase 1: 先铺开轨道（每轨 2 个）
  - 子轨 0-2: 各 2 个（用掉 6 个）
  - 剩余 1 个
- Phase 2: 按周长比例加密
  - 子轨 2（半径 24）周长最大 → 优先加密
  - 子轨 2: 2 + 1 = **3 个游侠**

**结果**：
```
子轨 0 (r=14): 2 个 ██
子轨 1 (r=19): 2 个 ██
子轨 2 (r=24): 3 个 ███  ← 第 7 个游侠加入这里
```

**第 7 个游侠的参数**：
- 半径 = 24
- group_idx = 2
- phase_offset = 2 * 4 // 3 ≈ 2
- 起始角 = 西南角（与前两个游侠错开）

### 添加第 8 个游侠

**算法重新分配**（8 个游侠）：
- Phase 1: 铺开 4 条子轨（每轨 2 个）

**结果**：
```
子轨 0 (r=14): 2 个 ██
子轨 1 (r=19): 2 个 ██
子轨 2 (r=24): 2 个 ██
子轨 3 (r=29): 2 个 ██  ← 新开一条子轨
```

---

## 📐 视觉化

### 俯视图（绕 Core 的同心方环）

```
           Core @ (530, -651)
                 ▲
                 │
    ┌────────────┼────────────┐
    │            │            │
    │   子轨 2 (r=24)         │  游侠 #5, #6
    │   ┌────────┼────────┐   │
    │   │        │        │   │
    │   │  子轨 1 (r=19)  │   │  游侠 #3, #4
    │   │  ┌─────┼─────┐  │   │
    │   │  │     │     │  │   │
    │   │  │ 子轨 0     │  │   │  游侠 #1, #2
    │   │  │  (r=14)    │  │   │
    │   │  │  ┌──●──┐   │  │   │  ● = Core
    │   │  │  │     │   │  │   │
    │   │  │  └─────┘   │  │   │
    │   │  └───────────┘  │   │
    │   └─────────────────┘   │
    └─────────────────────────┘
```

### 同一子轨内的相位错开

```
子轨 0 (半径 14)，2 个游侠：

    #1 (lane 0, phase 0)
         ●────────────────●
         │                │
         │                │
         │      Core      │
         │                │
         │                │
         ●────────────────●
                         #2 (lane 1, phase 2)

两个游侠从对角出发，永远相隔半圈
```

---

## 🎯 总结

### 关键概念

1. **中轨不是单轨，是多层同心结构**
   - 由多条不同半径的**子轨道**组成
   - 间隔 = 单位视野半径（游侠 5 格）

2. **lane 编号是局部索引**
   - 同一半径内区分 group_idx
   - 跨半径可以重复（游侠 #1 和 #5 都是 lane 0）

3. **外圈优先混合策略**
   - 优先铺开多条子轨（最大化领土覆盖）
   - 不在单轨上堆满 8 个单位
   - 剩余单位按周长比例回填外圈

4. **相位错开机制**
   - 同一子轨的多个单位通过 phase_offset 分散
   - 2 个单位 → 对角（相隔 180°）
   - 4 个单位 → 四角（均匀分布）
   - 8 个单位 → 四角 + 四边中点

### 你的 6 个游侠

✅ **分布在 3 条子轨道上**：
- 子轨 0（半径 14）：2 个游侠
- 子轨 1（半径 19）：2 个游侠
- 子轨 2（半径 24）：2 个游侠

✅ **形成分层防御**：
- 内层游侠（半径 14）贴近 Core，快速反应
- 外层游侠（半径 24）扩大警戒范围，提前发现敌人

✅ **每条子轨内通过 phase 错开**：
- 避免聚堆，提高覆盖效率
- 2 个游侠对角分布，互不干扰

### 后续扩展

**第 7 个游侠**：
- 加入子轨 2（半径 24），与前 2 个游侠错开相位

**第 8 个游侠**：
- 触发新子轨 3（半径 29），开始第 4 层防御

**第 9-20 个游侠**：
- 继续外扩子轨（半径 34, 39, 44, ...）
- 最终形成 10+ 层同心方环防御体系

---

## 📄 相关代码

### 轨道分配算法
`arena_hero_strategy.py:6920-6984` - `_lightning_calculate_outer_first_orbits`

### 单位到子轨映射
`arena_hero_strategy.py:6986-7052` - `_lightning_assign_orbit_lanes`

### 相位计算
`arena_hero_strategy.py:7054-7087` - `_lightning_orbit_waypoint`

---

**报告生成时间**：2026-08-10
**数据来源**：vps168 实时日志 + 本地代码分析
