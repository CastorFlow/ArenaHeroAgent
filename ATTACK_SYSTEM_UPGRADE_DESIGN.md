# 攻击系统升级设计稿 v0.1

> 状态：设计定稿，待实现
> 基线战况：vps168 tick 103100–105100（2026-08-14 凌晨）
> 目标分支：lightning（`arena_hero_strategy.py`）

## 0. 诊断：为什么是"被动反击"不是"攻击系统"

伤害只有两个来源，且都需要敌人先走到我方攻击范围里：

- 游侠 `shoot_cell`：半径 1–3 横/竖/45°对角直线、视野 5
- 先锋 `sweep`：相邻格

火控塔都建好了——`ShotLedger` 去重、T1–T4 威胁分级、漏斗、医疗轮换——但它们全部架在一个前提上：**敌人已经撞进射程**。敌人不进射程，游侠就一枪不开转而去巡逻，威胁在远距离不被削减，于是被一从东墙压到 Core 门口（r=38→6）。

### 三处根因

- **根因 A · 死锁**：`_predicted_enemy_cell` 只做一阶速度外推（相邻两帧差外推一格）。先锋在包围圈里上下往返时速度方向每帧反转，外推永远指向它刚离开的那格——"永远瞄错方向"。
- **根因 B · 镜像僵持**：敌我对射时双方都在等对方先动。没有"敌方预瞄/视野盲区"模型，没有"牺牲换血位"概念。
- **根因 C · 经济无存底**：`_select_spawn` 只留 reserve=2，几乎把资源全转成兵力。阵亡了游侠没资源当场补、残血游侠回 Core 没资源回血。

---

## 1. 机制一 · 围猎先锋：死锁破解

### 1.1 用"敌方轨迹库"替代一阶外推

维护敌方单位最近 **7 帧**位置（内存里已有 `recent_positions`，目前只存我方；把敌方也纳入），从真轨迹里识别三类规律：

| 规律 | 判别 | 定位功能 | 对命中率的意义 |
|---|---|---|---|
| ZIGZAG（往复） | 相邻帧方向频繁翻转 | 包围圈绕圈 | 不该再射"刚离开那格"，应射它正在绕向的对轴另侧 |
| LINEAR（直线逃离） | 方向连续 ≥2 帧不回弹 | 残血逃命 | 沿原方向外推 1–2 格截击 |
| CIRCLE（环绕） | 位置绕我 Core 或某单位 | 风筝 | 朝弧线内侧提前量，不是圆心 |

### 1.2 集火打分：惩罚"同一方向连射不中"

`shot_miss_counts` 现记每个 `(target_id, cell)` 的脱靶（8 帧记忆），但无"方向"概念，四格脱靶彼此独立，僵持里四格轮流脱靶永不收敛。改为**按轴聚合**：

- key = `(target_id, axis)`，8 帧内对同一轴线（南北/东西）脱靶数
- 候选压在**脱靶最少**/**最后命中**的方向，另一条轴线做备用

一个先锋只走 4 个方向，打中的必然落在这 4 格之一。排除已连续脱靶的方向，剩余方向命中概率必然提升（条件概率，不是空间平均）。

---

## 2. 机制二 · 敌方游侠：镜像僵持 → 对角换血 + 卡视野

### 2.1 换血位（对角线远位）—— 仅在"对峙僵局"触发

触发条件：我方游侠与敌方游侠互相等对方移动（对峙僵局）。

动作：从这条对角线此前没有我方单位、敌方游侠视野/预瞄一定面向现有阵型的方向，调一名游侠 N 切入对角远位并瞄准敌方游侠。敌方游侠没预瞄这个位置（上回合在防另一条线）。

| 回合 | 动作 | 敌方游侠处境 |
|---|---|---|
| 0 | 选 N 走对角线远位，准备瞄准 | 已盯着旧包围线，未防 N |
| 1 | N 站桩瞄准敌游侠（敌动弹会中枪） | 可移动格全被我方覆盖，动即中 |
| 1–2 | N 中 1 枪 → 回 Core 回血 | 再被围 → 换下一名来顶 |
| 2–3 | 接力游侠用同法再补一枪 | 两次换血后 HP 归零倒下 |

关键：受伤的只有 N，且 N 回 Core 回血安全（敌方游侠被牵制、我方 Core 满场）。

### 2.2 卡敌方视野盲区—— 可能无伤

SDK 只给 `visible_enemies`，不给敌方视野。我方自己算：以每个可见敌单位为中心，用敌方各兵种视野半径 + 障碍遮挡，枚举那格能看见的格集合，**补集即盲区**。站在盲区里的我方游侠，敌方视野内不出现 → 它根本不会预瞄我们，可能无伤命中。

**只对敌方游侠 + 先锋算盲区**（敌方工人不参与）。

复用现有 `_vision_line_clear`（2292 行，我方视野用的就是它），把观察者换成敌方单位、视野半径换成敌方兵种值即可。

盲区来源：① 障碍（石头）背后；② 敌方游侠朝别方向专注，背后弧段无防；③ 敌方视野半径有限（R3 工人看不到 R5 处游侠）。

---

## 3. 机制三 · Core 资源存底 150 + 产能兜底

`_select_spawn` 现留 reserve=2，几乎把资源全转成兵力。

### 3.1 人口 < 40 时资源上限本身装不下 150

| 人口 | 游侠价(round) | 资源上限 pop×5 | 上限−150 | 能否存 150 还留一个游侠 |
|---|---|---|---|---|
| 20 | 12 | 100 | −50 | ❌ |
| 30 | 26 | 150 | 0 | ❌ |
| 34 | 34 | 170 | 20 | ❌ |
| 40 | 34 | 200 | 50 | ⚠️ |
| 44 | 45 | 220 | 70 | ✅ |
| 50 | 59 | 250 | 100 | ✅ |

**死锁边界**：人口 ≤40 时硬守"存满 150 才造兵" → 上限−150 永远不够造游侠 → 永远不造兵 → 人口不涨 → 上限不涨 → 永久卡死。必须兜底。

### 3.2 两条规则（每 tick 现场算，不硬编码人口）

- **规则② · 产能兜底**：`capacity - 150 < unit_cost(RANGER)` 时，无视存底，继续按现有 3:1 / 阵亡补齐逻辑造兵。目的：抬人口→抬上限→摆脱低谷。
- **规则① · 资源存底**：`capacity - 150 >= unit_cost(RANGER)` 时，只有 `resources > 150` 的部分才可用来造兵。目的：留 150 给阵亡游侠当场复活回血、给残血游侠回 Core 治疗。

存底只在和平期/休整期起作用。开打（T3/T4、Core 受威胁）时按现有威胁分支优先生产挡战争的兵，存底让位。

---

## 4. 每 Tick 调度顺序（改动版）

| # | 环节 | 改动 |
|---|---|---|
| 1 | 轨道几何 / 威胁分级 | 不变 |
| 2 | 敌方轨迹库 | **新增**：7 帧窗口，识别 ZIGZAG/LINEAR/CIRCLE |
| 3 | 先锋死锁调度（按轴惩罚）| **改**：`shot_miss_counts` 从单格改为按轴聚合 |
| 4 | 敌方游侠战术（盲区/换血）| **新增**：算敌方游侠+先锋视野盲区；对峙僵局时从对角远位调 N |
| 5 | ShotLedger 全局射击分配 | 不变 |
| 6 | 医疗 / 漏斗 / 锚定 | 不变 |
| 7 | Core 生产 | **改**：`_select_spawn` 加入存底 150 + 产能兜底 |

---

## 5. 验收指标

- 命中率从 12% 提升到 25%+
- 围猎死锁消失（不再"两游侠 vs 一先锋三方卡死"长时停滞）
- 敌方游侠被无伤/低伤终结
- Core 不再因经济打空而补不上
- 不发生新死锁（人口压到 20–30 也能靠兜底继续出产能）
